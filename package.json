{
  "name": "colors",
  "author": "lumine-code",
  "description": "Show the colors written in a file and across the project.",
  "repository": "https://github.com/lumine-code/colors",
  "bugs": {
    "url": "https://github.com/lumine-code/colors/issues"
  },
  "keywords": ["swatch", "palette", "hex", "rgb", "hsl", "sass", "stylus"],
  "version": "1.0.0",
  "license": "MIT",
  "engines": {
    "lumine": "^1.0.0"
  },
  "backgroundTips": [
    "You can see every colour a project defines, grouped and sorted, with {{ 'colors:show-palette' | keystroke }}",
    "You can find every use of a colour across the project with {{ 'colors:find-colors' | keystroke }}"
  ],
  "main": "./lib/main",
  "files": ["docs", "keymaps", "lib", "menus", "spec", "styles"],
  "scripts": {
    "test": "lumine --test spec",
    "lint": "eslint . --max-warnings 0",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "css-color-function": "^1.3.3",
    "picomatch": "^4.0.5"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.8.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-n": "^18.2.2",
    "globals": "^17.9.0",
    "prettier": "^3.9.6"
  },
  "providedServices": {
    "autocomplete.provider": {
      "description": "Completes colour names and colour variables while typing.",
      "versions": {
        "1.0.0": "provideAutocomplete"
      }
    },
    "colors.project": {
      "description": "Exposes the scanned colour project: its palette, its variables and its buffers.",
      "versions": {
        "1.0.0": "provideColorsProject"
      }
    }
  },
  "consumedServices": {
    "color-picker": {
      "versions": {
        "^1.0.0": "consumeColorPicker"
      }
    },
    "colors.color-expressions": {
      "versions": {
        "^1.0.0": "consumeColorExpressions"
      }
    },
    "colors.variable-expressions": {
      "versions": {
        "^1.0.0": "consumeVariableExpressions"
      }
    }
  },
  "viewProviders": ["colorsViewProvider"],
  "deserializers": {
    "Palette": "deserializePalette",
    "ColorSearch": "deserializeColorSearch",
    "ColorProject": "deserializeColorProject",
    "ColorProjectElement": "deserializeColorProjectElement",
    "VariablesCollection": "deserializeVariablesCollection"
  },
  "configSchema": {
    "markerType": {
      "title": "Marker Type",
      "description": "How a colour is drawn in the editor.",
      "type": "string",
      "enum": [
        { "value": "background", "description": "Background — the text sits on the colour" },
        { "value": "underline", "description": "Underline — a rule beneath the text" },
        { "value": "outline", "description": "Outline — a box around the text" },
        { "value": "dot", "description": "Dot — a round mark before the text" },
        { "value": "square-dot", "description": "Square dot — a square mark before the text" },
        { "value": "gutter", "description": "Gutter — a mark in its own gutter" }
      ],
      "default": "background"
    },
    "supportedFiletypes": {
      "title": "Supported File Types",
      "description": "File extensions whose colours are marked. A `*` marks colours everywhere.",
      "type": "array",
      "items": { "type": "string" },
      "default": ["*"]
    },
    "filetypesForColorWords": {
      "title": "File Types for Colour Words",
      "description": "File extensions where a bare name such as `red` or `whitesmoke` counts as a colour. Use a value matching no extension to switch colour words off entirely.",
      "type": "array",
      "items": { "type": "string" },
      "default": ["css", "less", "styl", "stylus", "sass", "scss"]
    },
    "ignoredScopes": {
      "title": "Ignored Scopes",
      "description": "Regular expressions matching scopes whose colours are left unmarked. `\\.comment` skips every comment.",
      "type": "array",
      "items": { "type": "string" },
      "default": []
    },
    "ignoredBufferNames": {
      "title": "Ignored Buffer Names",
      "description": "Glob patterns of files that get no colour marks at all.",
      "type": "array",
      "items": { "type": "string" },
      "default": []
    },
    "maxDecorationsInGutter": {
      "title": "Maximum Marks per Gutter Row",
      "description": "How many colours one row of the gutter may show.",
      "type": "integer",
      "minimum": 1,
      "maximum": 100,
      "default": 100
    },
    "delayBeforeScan": {
      "title": "Delay Before Scan",
      "description": "Milliseconds of quiet after typing before the buffer is scanned again. The wait restarts on every keystroke.",
      "type": "integer",
      "minimum": 0,
      "default": 500
    },
    "sourceNames": {
      "title": "Source Names",
      "description": "Glob patterns of the files scanned for colour variables.",
      "type": "array",
      "items": { "type": "string" },
      "default": ["**/*.css", "**/*.styl", "**/*.stylus", "**/*.less", "**/*.sass", "**/*.scss"]
    },
    "ignoredNames": {
      "title": "Ignored Names",
      "description": "Glob patterns skipped when scanning the project for variables.",
      "type": "array",
      "items": { "type": "string" },
      "default": [
        "**/vendor",
        "**/node_modules",
        "**/spec",
        "**/test",
        "**/font",
        "**/img",
        "**/icon"
      ]
    },
    "extendedSearchNames": {
      "title": "Extended Search Names",
      "description": "Extra glob patterns searched by Find Colors, on top of the source names.",
      "type": "array",
      "items": { "type": "string" },
      "default": ["**/*.css"]
    },
    "ignoreVcsIgnoredPaths": {
      "title": "Ignore VCS Ignored Paths",
      "description": "Skip files the repository ignores.",
      "type": "boolean",
      "default": true
    },
    "traverseIntoSymlinkDirectories": {
      "title": "Traverse Into Symlinked Directories",
      "description": "Follow symlinked directories when scanning the project.",
      "type": "boolean",
      "default": false
    },
    "autocompleteScopes": {
      "title": "Autocomplete Scopes",
      "description": "Scopes in which colour names and variables are offered as completions.",
      "type": "array",
      "items": { "type": "string" },
      "default": [
        ".source.css",
        ".source.css.less",
        ".source.sass",
        ".source.css.scss",
        ".source.stylus"
      ]
    },
    "extendAutocompleteToVariables": {
      "title": "Complete Non-Colour Variables",
      "description": "Offer variables that do not hold a colour as completions too.",
      "type": "boolean",
      "default": false
    },
    "extendAutocompleteToColorValue": {
      "title": "Complete Colour Values",
      "description": "Offer the colour's value alongside its name.",
      "type": "boolean",
      "default": false
    },
    "autocompleteSuggestionsFromValue": {
      "title": "Complete Variables From a Value",
      "description": "Suggest colour variables when a hexadecimal value is typed.",
      "type": "boolean",
      "default": false
    },
    "sortPaletteColors": {
      "title": "Sort Palette Colours",
      "description": "The order colours appear in the palette.",
      "type": "string",
      "enum": [
        { "value": "none", "description": "None — the order they were found in" },
        { "value": "by name", "description": "By name" },
        { "value": "by color", "description": "By colour" }
      ],
      "default": "none"
    },
    "groupPaletteColors": {
      "title": "Group Palette Colours",
      "description": "How the palette is divided up.",
      "type": "string",
      "enum": [
        { "value": "none", "description": "None — one flat list" },
        { "value": "by file", "description": "By the file each colour is declared in" }
      ],
      "default": "none"
    },
    "mergeColorDuplicates": {
      "title": "Merge Duplicate Colours",
      "description": "Show one entry for colours whose value is the same.",
      "type": "boolean",
      "default": false
    },
    "sassShadeAndTintImplementation": {
      "title": "Sass Shade and Tint Implementation",
      "description": "Sass ships no `shade` or `tint`, and the two common libraries define them differently.",
      "type": "string",
      "enum": [
        { "value": "compass", "description": "Compass" },
        { "value": "bourbon", "description": "Bourbon" }
      ],
      "default": "compass"
    },
    "notifyReloads": {
      "title": "Notify on Reload",
      "description": "Report what changed when the Reload command finishes.",
      "type": "boolean",
      "default": true
    },
    "dismissableReloadNotifications": {
      "title": "Reload Notifications Stay Until Dismissed",
      "description": "Keep the reload notification on screen until it is closed by hand.",
      "type": "boolean",
      "default": true
    }
  }
}
